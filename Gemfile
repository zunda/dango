source 'https://rubygems.org'
gem 'sinatra', :github => 'sinatra/sinatra'
gem 'thin'
gem 'rack-timeout', :github => 'heroku/rack-timeout'

group :development, :test do
	gem 'rails_best_practices'
end

group :test do
	gem "capybara"
end
